// TODO: Run this file in chrome. I mean link this with index.html and run using live server. 

// console.log(this); // Window object return karega yeh.

// Output will look like this :- Window {0: Window, window: Window, self: Window, document: document, name: '', location: Location, …} (Only in chrome not in vs terminal)

// console.log(window);  // Dont try this in VS code terminal error ayega. Try in chrome console.
// Output :- Window {0: Window, window: Window, self: Window, document: document, name: '', location: Location, …}  (We'll get same thing as we got in printing "this" )

// this === window  // execute this in terminal of chrome. It will return true. 

// function myFunc(){
//     // We can use "use strict" to stop showing window object.    
//     // "use strict"   // if used it will return undefined.
//     console.log(this);  // this returns the window object.
// }
// myFunc();   // here calling this function means calling window.myFunc()


