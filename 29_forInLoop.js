// We dont use this loop and while loop as much. 
// for-in loop in JavaScript 
// in matlab index 😂 (Just Kidding)

let arr = ["Pratik","Rohan","Venkatesh","Raju","Baburao","Shyam"];

for(index in arr){
    // console.log(index);   // it will print the index of an array But we want values Right ? 
    // console.log(arr[index]);
    // console.log(arr[index].toUpperCase());
}